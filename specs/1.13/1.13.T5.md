---
id: 1.13.T5
title: Definition of done + ship v1.0.1
---

## Goal
Close the spec and cut the patch release so plugin users get the standalone fixes via `/plugin update`.

- Reconcile deferrals (none expected); `hooks.test.sh` green; feature-completion checklist.
- Mark 1.13 DONE in the index + archive `specs/1.13/` → `specs/archive/1.13/`.
- Bump `.claude-plugin/plugin.json` `version` → `1.0.1`; commit.
- `/flow:ship`: preflight, push `main`, wait for CI green, tag `v1.0.1`, push tag.

## Done when
- [ ] Index → DONE, detail archived; `resolved` green.
- [ ] `plugin.json` = 1.0.1; `v1.0.1` tag pushed; CI green on the release commit.
- [ ] Changelog notes the two standalone fixes + the purge tool.

## Notes
Patch bump (fixes only, no new feature). After publish, `/plugin update` in each profile pulls the fixes.
