---
id: 1.13.T4
title: Apply purge to both profiles + verify plugin-only
---

## Goal
Bring the maintainer's machine to the target state (plugin-only, both accounts) and prove the fixed plugin stands alone.

- Run `uninstall.sh` to purge manual-install artifacts from every detected profile.
- `~/.claude` has no plugin yet — it needs a one-time user `/plugin install flow@flow-toolkit`; if absent, purging it would leave no toolkit, so **skip/flag `~/.claude` until the plugin is installed there** (report it, don't strip it bare).
- Verify plugin-only in a profile that has the plugin: scoped agent dispatch resolves with bare copies gone; the four hooks fire (single, not doubled); `/flow:ship`/`/flow:lint` find `flow-preflight.sh` via `${CLAUDE_PLUGIN_ROOT}`.

## Done when
- [ ] `.claude-company` (plugin present) purged of manual artifacts; plugin-only confirmed (no bare `flow-*`; guards fire once; scoped dispatch works).
- [ ] `~/.claude` handled explicitly: purged if the plugin is installed there, else reported as "install plugin first" (not left bare).
- [ ] A short verification checklist recorded (what proved each: dispatch, hooks, preflight).

## Notes
Full end-to-end of the *fixed* plugin needs v1.0.1 published + `/plugin update` (the installed cache is v1.0.0 until T5 ships) — verify the fixes via `claude --plugin-dir .` where a live plugin session is needed.
