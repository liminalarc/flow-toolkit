---
id: 1.7
title: Sub-agent catalog + autonomy controls + verifier gating
links: [1.2, 1.6]
---

## Problem
The toolkit runs everything in one context and always pauses for plan approval. Spec 1.2 ratified a sub-agent catalog (isolated context) and a layered autonomy model with a verifier safety net, so routine work can run with less hand-holding without losing control of direction.

## Value
As a flow user I want per-task implementer agents whose diffs an independent verifier checks before integration, and a `checkpoint`/`auto-build` control, so that large specs build in parallel and `auto-build` is tolerable because nothing merges unverified.

## Scope
**In:** implementer + verifier sub-agent definitions; autonomy front-matter (`autonomy: checkpoint|auto-build`, default `checkpoint`); `.flow-toolkit.json` `autonomy.default` + `autonomy.force`; precedence project-force > repo-default > per-spec; verifier gating mode-dependent (D3: blocking auto-build / advisory checkpoint), single verifier, one bounded retry then escalate; wiring into `/flow`'s build loop.
**Out:** reviewer/researcher agents (ship with their skills, 1.8/1.9/1.11); task-file model (1.6).

## Acceptance criteria
- [ ] Implementer + verifier agents defined (purpose, tools, R/W, invocation) per the 1.2 catalog.
- [ ] Autonomy resolved with correct precedence; `auto-build` never bypasses Claude Code permissions or self-approves.
- [ ] Verifier gates per D3: blocking under auto-build, advisory under checkpoint; failing auto-build → 1 retry → escalate.
- [ ] `/flow` build loop dispatches one implementer per task and one verifier before integration.

## Plan (thin slices)
1. [x] Autonomy resolver: `flow-preflight.sh autonomy <spec> [--repo D]` → `checkpoint|auto-build`, precedence force>per-spec>default>`checkpoint`; `hooks.test.sh` cases. -> d6991e5
2. [x] `agents/flow-implementer.md` + `agents/flow-verifier.md` per the 1.2 catalog (tools/R-W/contract). -> (this commit)
3. [ ] Installers copy `agents/*.md` into each profile (install.sh + install.ps1 in lockstep) + README install steps. -> <sha>
4. [ ] Wire `/flow`: §2 resolve autonomy + gate the plan-approval pause; §3 dispatch implementer→verifier, D3 gating + retry/escalate. -> <sha>
5. [ ] Docs: README (autonomy/agents/gating), preflight header 3→4 rules, root CLAUDE.md structure, spec AC/progress. -> <sha>

## Decisions
- 2026-07-14 — **Autonomy config lives in the existing `.flow-toolkit.json`** (hook-config surface), not a new file. `.flow/config.yml` is flow's backend config; `.flow-toolkit.json` already holds toolkit/hook config (`spec.maxLines`), so `autonomy.default`/`autonomy.force` belong there. Matches the spec's own naming.
- 2026-07-14 — **Precedence resolution is a `flow-preflight.sh` subcommand**, not prose in `/flow` (user decision). Deterministic, single-source, `hooks.test.sh`-covered — the "correct precedence" AC becomes machine-verifiable. Unknown/invalid value → advisory + safe default `checkpoint`.
- 2026-07-14 — **Precedence = `force` > per-spec front-matter > `default` > builtin `checkpoint`** (user decision). `autonomy.force` is a hard project override; `autonomy.default` is only the fallback when the spec is silent; a spec's own `autonomy:` beats the repo default. Keeps `force`/`default` distinct (the scope line's "force>repo-default>per-spec" was shorthand).
- 2026-07-14 — **Agents distributed via both installers now** (user decision): `agents/*.md` copied into each profile so `/flow` can dispatch them and 1.7 is end-to-end smoke-testable. 1.10 later moves distribution to the plugin; installer is the mechanism until then.
- 2026-07-14 — **Verifier reads the diff** from the implementer's worktree (`git diff`, parallel path) or the uncommitted working-tree diff (inline path); read-only, judges never fixes (catalog + D3).

## Verification / evidence
- AC1 — agent files parse as Claude Code sub-agents; tools/R-W match the 1.2 catalog table.
- AC2 — `hooks.test.sh` autonomy cases prove precedence; docs confirm `auto-build` gates only the plan-approval pause (no permission bypass / self-approval).
- AC3 — gating cases: verifier blocking under `auto-build` (fail→retry→escalate), advisory under `checkpoint`.
- AC4 — smoke-test `/flow` on a scratch spec: one implementer per task, verifier before integration.

## Progress log
- 2026-07-14 `d6991e5` — Slice 1: autonomy resolver in `flow-preflight.sh` (precedence force>per-spec>default>checkpoint), 9 `hooks.test.sh` cases green (65 total).
