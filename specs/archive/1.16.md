---
id: 1.16
title: Persisted, self-maintaining project validation rubric — .flow/validate/*.md
links: [1.14, 1.15]
---

## Problem
1.14's `flow-ux-validator` takes project specifics (the design system) as per-run args or infers them each
time — wasteful, and it never produces a rubric that *improves*. A project's design system is
stable-but-drifting; it deserves a **persisted, engineer-owned rubric** the agent reads instead of
re-deriving. This also gives 1.15's done-gate a repeatable source for the design-system layer without
re-passing args every build.

## Value
As an engineer directing agentic delivery I want the project's UI/UX rubric captured once in the repo and kept
current, so `flow-ux-validator` scores against a stable, improving standard instead of re-inferring it — and
so the done-gate has something durable to check against.

## Scope
**In:**
- A persisted project rubric in the target repo — `.flow/validate/ui.md` + `.flow/validate/ux.md` (the
  **project layer only**; baseline heuristics stay in the toolkit).
- **Bootstrap on first run** — absent rubric ⇒ the agent infers a draft from source and returns it as a
  *proposed* draft; the main thread **saves only on engineer approval**. Never silently authoritative.
- **Drift-refresh** — later runs cheaply detect the design system moved (fingerprinted `basis:` files) and
  **offer** an update. Never overwrite the curated rubric silently.
- `flow-ux-validator` reads `.flow/validate/*.md` when present (the project layer of its rubric); the
  `/flow:validate` skill + 1.15's gate consume it.

**Out:**
- The agent + standalone skill (1.14); the done-gate (1.15).
- Baseline heuristics (stay in `skills/validate/reference/`).
- Auto-editing app code to conform (validation only, never fixes).

## Acceptance criteria
- [x] Absent rubric ⇒ agent proposes a source-inferred draft; saved only on engineer approval — proven on a scratch app
- [x] Present rubric ⇒ agent reads it as the project layer (no re-inference) — proven
- [x] Drift detected ⇒ agent surfaces the change and offers a refresh; no silent overwrite — proven
- [x] Docs updated (guide, README) — inspection

## Plan (thin slices)
1. [x] Helper (TDD): `hooks.test.sh` cases first, then `rubric-drift`/`rubric-basis` in `flow-preflight.sh` -> `05f690b`
2. [x] `skills/validate/reference/rubric.md` — file template (`basis:` + `generated:`) + bootstrap/persist/refresh protocol -> `b7e2060`
3. [x] `agents/flow-ux-validator.md` — rubric precedence (`.flow/validate/<lens>.md` merged w/ baseline; else infer + return proposed draft), report drift; still never writes project tree -> `b7e2060`
4. [x] `skills/validate/SKILL.md` — main-thread persist lifecycle (save on approval, offer refresh on drift, confirm-first) -> `b7e2060`
5. [x] `skills/run/reference/implement.md` §4 — gate reads `.flow/validate/*` + drift-check + nudge to `/flow:validate` -> `b7e2060`
6. [x] Docs — guide, README, `how-it-works`, CLAUDE.md + hooks/CLAUDE.md (five preflight rules, lockstep) -> `49a18a7`

## Decisions
- 2026-07-18 — Split from 1.14: rubric *lifecycle* (bootstrap / persist / refresh) is a distinct capability
  from the agent that *uses* the rubric. Persisted at `.flow/validate/*.md` in the target repo —
  engineer-owned, agent-proposed. Serves both the standalone skill (1.14) and the done-gate (1.15).
- 2026-07-18 — **Read-only seam:** the agent proposes (reads present rubric / returns inferred draft / flags
  drift); the **main thread persists** every write, confirm-first. Preserves 1.14's read-only-re-project boundary.
- 2026-07-18 — **Drift = fingerprinted basis** (`sha256`, first 12) recorded in rubric front-matter; re-hash to
  detect. Cheap + deterministic vs re-inferring each run.
- 2026-07-18 — **D3=A:** drift logic centralized in `flow-preflight.sh` (`rubric-drift` + `rubric-basis`),
  TDD-covered — the "a rule lives in one place" pattern; three consumers (agent, `/flow:validate`, gate) share it.
- 2026-07-18 — **Lifecycle division:** interactive bootstrap/refresh authoring lives in `/flow:validate`; 1.15's
  gate stays lean — reads + drift-checks, then nudges to `/flow:validate` rather than duplicating persist logic.
- 2026-07-18 — `.flow/validate/*.md` is **committed** (engineer-owned standard, like `.flow/config.yml`).

## Verification / evidence
Smoked end-to-end on a scratch static checkout app (Playwright v1.61, `design/tokens.css`):
- **AC1 (bootstrap):** dispatched the real `flow-ux-validator` (UI lens) with no `.flow/validate/ui.md` — it
  drove the app, inferred a draft from `tokens.css`, returned a *proposed* draft + basis, and wrote **nothing**
  to the project tree. Main thread then persisted it on approval, stamping `basis:` via `rubric-basis`;
  `rubric-drift` → in-sync (exit 0).
- **AC2 (present):** re-dispatched with the rubric present — the agent read it as the project layer, cited its
  clauses, and did **not** re-infer.
- **AC3 (drift):** edited `tokens.css` → `rubric-drift` exit 2 naming the changed file; the agent reported
  `rubric status: DRIFT`, identified the now-stale clause, and did not overwrite. Approved-refresh re-stamp via
  `rubric-basis` cleared drift (exit 0).
- **Helper (TDD):** `hooks.test.sh` 65→79 (14 new cases: `rubric-basis` emit + missing-file error;
  `rubric-drift` in-sync/CHANGED/MISSING/no-basis/absent-rubric). Full suite green; no orphan dev server.
- **AC4:** guide §2.7 + `/flow:validate` section + ToC; README row; how-it-works validate + hooks diagrams;
  root CLAUDE.md + hooks/CLAUDE.md preflight-rule list (lockstep).

## Progress log
- 2026-07-18 — spec created (re-homed from 1.14's rubric-maintenance question)
- 2026-07-18 `8c213b9` — fleshed out detail, set IN PROGRESS
- 2026-07-18 `05f690b` — `rubric-basis`/`rubric-drift` preflight subcommands (TDD, 14 cases)
- 2026-07-18 `b7e2060` — rubric lifecycle: reference protocol, agent reads/proposes, skill persists, gate nudges
- 2026-07-18 `49a18a7` — docs across guide/how-it-works/README/CLAUDE (five preflight rules, lockstep)
