---
id: 1.10
title: Plugin packaging + criterion-gated installer transition
links: [1.2, 1.11]
---

## Problem
Distribution today is `git clone` + force-copy installer + `git pull` — no versioning, and updates need a manual reinstall + restart across every profile. Spec 1.2 (D5) chose a Claude Code plugin as primary distribution, installer retained for hooks + fallback, retired criterion-gated. Research during this spec found plugins **always namespace** multi-skill invocations (`/plugin:skill`) — so a plugin can't preserve the bare `/flow` names. Decision: embrace the namespace under a short plugin name.

## Value
As a flow user across several projects and two profiles I want the toolkit as a **versioned Claude Code plugin** with a clean namespaced command set (`/flow:run`, `/flow:hunt`, …), so updates are reliable/reversible (no more `git pull` + reinstall + restart) — with a clear migration path off the old bare commands.

## Scope
**In:**
- `.claude-plugin/plugin.json` (`name: flow`, versioned) + `.claude-plugin/marketplace.json` (this GitHub repo is the marketplace).
- **Rename to the new scheme** — skills `flow`→`run`, `flow-hunt`→`hunt`, `flow-review`→`review`, `flow-pr`→`pr`; commands `flow-init`→`init`, `flow-lint`→`lint`, `flow-ship`→`ship`; update every `name:` front-matter + all internal cross-references. Agents keep `flow-*` names (internal dispatch, not user-facing).
- Plugin carries hooks via `"${CLAUDE_PLUGIN_ROOT}"/hooks/*.sh`; verify all four fire under the plugin on both profiles (**D5 exit criterion**).
- Retire the installer to a documented **legacy fallback**; add stale-file prune (**resolves the [[1.11]] deferral**); `install.{sh,ps1}` stay in lockstep.
- Docs: README (plugin install, new commands, migration, exit criteria) + CLAUDE.md (distribution architecture, `.claude-plugin/` structure) + a migration guide for existing projects.

**Out:** skill/agent *behavior* (unchanged — this repackages + renames, no new features).

## Acceptance criteria
- [x] `/plugin marketplace add liminalarc/flow-toolkit` + `/plugin install flow@…` installs the toolkit — verified live via `claude --plugin-dir .`: plugin loads, `plugin details flow` clean. (Marketplace channel activates on push.)
- [x] All 7 entry points work under the plugin — verified live: session lists `flow:run|hunt|init|lint|ship|review|pr`; `flow-reviewer` **dispatched by bare name** from a skill context (the top risk — resolved); content is rename-only.
- [x] All four hooks fire from the plugin — verified live via `${CLAUDE_PLUGIN_ROOT}/hooks`: session-brief on start, commit-guard blocked a bad message (PreToolUse), spec-guard blocked a status-carrying detail file (PostToolUse). D5 exit criterion met.
- [x] Installer retired to a documented fallback; installers in lockstep; stale bare command/skill files pruned (1.11 deferral → resolved). — prune exercised on both profiles (flow* removed, non-toolkit untouched); install.ps1 parses; lockstep confirmed.
- [x] README + CLAUDE.md + migration guide updated; transition exit criteria documented (no calendar date).

## Plan (tasks)
1. [x] **T1** — plugin manifest + marketplace + rename to the new scheme; `--plugin-dir` load verifies every entry point. -> 6515aee
2. [x] **T2** — hooks into the plugin (`${CLAUDE_PLUGIN_ROOT}`); verify all four fire on both profiles. -> 40a92b2
3. [x] **T3** — retire installer to fallback + prune stale files + lockstep + migration path. -> 3c9721d
4. [x] **T4** — docs: README, CLAUDE.md, migration guide, exit criteria. -> 5ede186

## Decisions
- 2026-07-15 — **Embrace plugin namespacing (Option A)** over keeping the installer primary. Plugins always namespace multi-skill invocations; a single plugin is the clean packaging. (User.)
- 2026-07-15 — Plugin **name `flow`**; primary command renamed `flow`→`run` to avoid the redundant `/flow:flow`; drop the `flow-` prefix from all skills/commands → `/flow:run|hunt|init|lint|ship|review|pr`. (User.)
- 2026-07-15 — **Plugin owns hooks**; installer retired to documented fallback; retirement gated on verified hook firing across both profiles (D5 criterion). (User.)
- 2026-07-15 — Agents keep `flow-*` names (internal, not user-facing); verify plugin dispatch works under `--plugin-dir`.
- 2026-07-15 — Revises the original 1.10 AC (which assumed a plugin could keep bare names) per the namespacing finding; migrated flat → dir form (4 tasks).
- 2026-07-15 — Relocated `commands/CLAUDE.md` → `docs/authoring-commands.md`: a plugin ships every `commands/*.md` as a command, so the authoring doc leaked as a stray `/flow:CLAUDE`. An explicit `commands:` array in `plugin.json` (to filter it) *broke plugin loading* — so relocation, not manifest filtering.
- 2026-07-15 — One `hooks/hooks.json` on the `${CLAUDE_PLUGIN_ROOT}/hooks` prefix serves both consumers: the plugin expands the var natively; the fallback installer substitutes it with the profile hooks dir. Avoids a second file / drift.
- 2026-07-15 — Left `~/.claude/commands/CLAUDE.md` (a pre-plugin installer artifact) out of the auto-prune — a `CLAUDE.md` could be user-authored; migration guide lists it as safe manual cleanup instead.

## Verification / evidence
- **Static (done):** `claude --plugin-dir . plugin details flow` → Skills(7) hunt/init/lint/pr/review/run/ship, Agents(5), Hooks(3), no stray CLAUDE; both manifests valid JSON; all 4 hook scripts resolve+run via `${CLAUDE_PLUGIN_ROOT}/hooks`; installer prune removed `flow*` from both profiles leaving non-toolkit skills intact; `install.ps1` parses; `hooks.test.sh` 65/0. T1 diff verified PASS by `flow-verifier`.
- **Live-verified (2026-07-15, `claude --plugin-dir .`):** plugin loads with the full inventory; `flow-reviewer` dispatched by **bare** name from a skill context (bare→plugin-namespaced resolution works, so the skills' bare agent refs are fine); `flow-commit-guard` blocked a bad commit message and `flow-spec-guard` blocked a status-carrying detail file — both via the `${CLAUDE_PLUGIN_ROOT}` path — and the session-brief fired at start. Loose end found + fixed: hook scripts still printed old bare command names in user-facing strings (`548374c`).

## Progress log
- 2026-07-15 `af46529` — plan + dir-form spec, IN PROGRESS.
- 2026-07-15 `6515aee` — T1: `.claude-plugin/{plugin,marketplace}.json`; renamed skills→run/hunt/review/pr, commands→init/lint/ship; swept cross-refs; relocated commands/CLAUDE.md→docs/. Verifier PASS.
- 2026-07-15 `40a92b2` — T2: hooks unified on `${CLAUDE_PLUGIN_ROOT}`; installer substitutes same token (lockstep).
- 2026-07-15 `3c9721d` — T3: installer demoted to fallback + stale-file prune (resolves 1.11 deferral); lockstep.
- 2026-07-15 `5ede186` — T4: plugin-first README + migration guide + exit criteria; CLAUDE.md distribution/tree; ship.md bumps plugin.json version.
- 2026-07-15 `548374c` — fix: swept stale bare command names out of hook user-facing strings (found during live validation).
- 2026-07-15 — live-verified the plugin (load, bare-name agent dispatch, all hooks fire via `${CLAUDE_PLUGIN_ROOT}`); AC1-3 confirmed; marked DONE + archived.
