---
id: 1.17
title: Descriptive spec filenames
links: [1.6]
---

## Problem
`specs/1.16.md` names nothing. In a file tree, an editor tab, a `git log --stat`, or a
PR's changed-files list, every spec is an opaque number — you must open it or cross-
reference the index to know what it is. The index already carries a free-text Title;
the filesystem never sees it. In ado mode a real story title sits on the card and never
reaches disk. Shipped = a spec's filename says what it is, in both shapes, without
breaking any existing bare-id spec anywhere.

## Value
As an engineer scanning a repo or a diff, I want each spec file's name to state what it
covers, so I can navigate the backlog without opening files or consulting the index.

## Scope
**In:** stem form `<id>-<slug>` for flat, orchestrator, and task files; guard accepts both
forms; one shared id→path resolver; slug inference (Title / `System.Title`);
`flow.spec_filename` knob; opt-in `/flow:lint --rename`; docs.
**Out:** automatic renaming (opt-in only); slugs in commit tags (`[<id>]` stays bare —
`/flow:ship` derives releases from it); changing the index entry format (already has Title).

## Acceptance criteria
- [x] `specs/<id>-<slug>.md` with front-matter `id: <id>` passes the spec guard; a genuine
      id/stem mismatch still blocks — proven by `hooks.test.sh` cases both ways
- [x] Dir form `specs/<id>-<slug>/` holds `<id>-<slug>.md` + `<id>-<slug>.T<n>-<ctx>.md`;
      the task-file local-AC nudge still fires on the slugged task stem — `hooks.test.sh`
- [x] `flow-preflight.sh spec-path <id>` resolves all 8 cases (flat|dir × plain|slugged ×
      active|archive), exits non-zero on not-found and on ambiguity (two slugged matches for
      one id) — `hooks.test.sh`
- [x] `to_resolves` + `cmd_resolved` call `spec-path`; no inline path lookup remains
- [x] Deferral `DONE`-gate matches flat ids (`642103`), not just dotted — regression case
- [x] `.flow/config.yml` `flow.spec_filename: id-slug|id` (default `id-slug`) selects the form
      new specs are authored in; hooks read no knob
- [x] `/flow:lint --rename [<id>|--all]` git-mv's specs to slugged names and rewrites index
      links, confirm-first, never unprompted; an already-slugged repo is a no-op
- [x] Self-migration: `/flow:lint --rename 1.17` renames this spec's own dir + 5 task files
      and the index link; `hooks.test.sh` green after
- [x] README, `docs/guide.md`, `docs/how-it-works.md`, root + `hooks/CLAUDE.md` state the
      filename form and the knob

## Plan (thin slices)
1. [x] T1 — guard accepts `<id>[-<slug>]` stems + slugged task detection -> `31e5188`
2. [x] T2 — `spec-path` resolver; refactor both call sites; fix flat-id DONE grep -> `646b29d`
3. [x] T3 — slugify rules, `flow.spec_filename` knob, authoring templates -> `a51bce7`
4. [x] T4 — `/flow:lint --rename`; dogfood on 1.17 -> `74dd63b`
5. [x] T5 — docs sweep -> `1db8164`

## Decisions
- 2026-07-29 — Slug on **every** stem (dir, orchestrator, task) rather than only the dir,
  keeping 1.6's rule that a spec dir is named for its orchestrator's stem.
- 2026-07-29 — Identity is the front-matter `id:`, never parsed out of the filename, so ids
  containing `-` (`BL-12`) stay unambiguous; the guard validates the stem *against* `id:`.
- 2026-07-29 — Config knob accepted despite CLAUDE.md's "no knobs until a second use":
  multi-project use means some repos want bare ids. Confined to `.flow/config.yml`
  (prompt-side); hooks accept both forms unconditionally so they gain no parsing or latency.
- 2026-07-29 — Slug caps: 40 chars for a spec, 20 for a task suffix, truncated at a word
  boundary — bounds total path length against Windows' 260-char limit.
- 2026-07-29 — Slugify drops **trailing stopwords** after truncation. Found by dogfooding:
  T5 "Docs sweep for the filename form" truncated to a dangling `docs-sweep-for-the`.
- 2026-07-29 — Fixed a latent bug in the code T2 refactored: `cmd_resolved`'s DONE-set grep
  required a dotted id, so ado flat ids (`642103`) never entered the set and the deferral
  `DONE`-gate was silently inert for every ado project. Regression case added.

## Verification / evidence
- `bash hooks/hooks.test.sh` — 114 passed / 0 failed (92 → 114; +12 guard cases, +10
  spec-path/resolved cases). CI runs the same harness.
- AC1/2 via guard cases both ways, incl. the false-accept guards (id `4.1` rejects stem
  `4.15`; task `T1` rejects stem `T9`) and a dashed id (`BL-12-backlog-item`).
- AC3 all 8 combinations, plus exit 1 not-found, exit 2 ambiguity naming both claimants,
  and `--spec-dir`. Also exercised against this repo: resolves the renamed slugged dir-form
  (1.17), an archived dir-form (1.13), and an archived flat spec (1.5).
- AC4 `grep -c '$dir/$id\|$_dir/$_to' hooks/flow-preflight.sh` = 0.
- AC8 self-rename: 6 `git mv`s, all recorded by git as renames (`git log --follow` reaches
  the pre-rename commit). Guard exit 0 on the orchestrator + all 5 slugged task files;
  `spec-path`, `resolved`, and `autonomy` all correct against the new names.
- **Version skew is real and ordering matters:** the repo's guard accepts the slugged names
  (exit 0) while the *installed* plugin v1.3.0 guard rejects them (exit 2). A project must
  `/plugin update flow` **before** running `/flow:lint --rename`, or its own guard will
  block edits to the renamed specs.

## Progress log
- 2026-07-29 `c73cb6b` — spec + 5 task files; index entry IN PROGRESS
- 2026-07-29 `31e5188` — T1 guard accepts `<id>-<slug>` stems + slugged task detection
- 2026-07-29 `646b29d` — T2 `spec-path` single-source resolver; flat-id DONE-grep fix
- 2026-07-29 `a51bce7` — T3 slugify rule, `flow.spec_filename` knob, authoring templates
- 2026-07-29 `74dd63b` — T4 `/flow:lint --rename` + self-rename of this spec
- 2026-07-29 `1db8164` — T5 docs sweep (README, guide, diagrams, both CLAUDE.md)
