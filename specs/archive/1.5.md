---
id: 1.5
title: CI — run hook tests on push/PR
links: [1.4]
---

## Problem
`/flow-ship`'s CI gate reports `N/A — no CI`; hook regressions are caught only if someone remembers to run `hooks.test.sh` locally.

## Value
As a maintainer, I want `hooks.test.sh` to run automatically on every push/PR so that the `/flow-ship` CI gate is real and hook regressions are caught before a release.

## Scope
**In:** a minimal `.github/workflows/test.yml` running `bash hooks/hooks.test.sh` on push + PR to `main` — single ubuntu job, native bash, no extra deps.

**Out:** testing the `.md` commands (they're prompts, no unit tests); release/publish automation (a release stays a manual tag per CLAUDE.md); OS matrix (hooks are POSIX; Windows Git Bash parity stays covered by the portability rule, not CI).

## Acceptance criteria
- [x] `.github/workflows/test.yml` runs `bash hooks/hooks.test.sh` on push + PR to `main`; green on the current tree.
- [x] Workflow is minimal — one job, ubuntu, no extra dependencies.
- [x] README notes CI runs the hook tests; CLAUDE.md/Project Structure reflect that CI now exists.
- [x] Verified end-to-end — push the branch, watch the run via `gh`, confirm it passes.

## Plan (thin slices)
1. [x] Write `.github/workflows/test.yml` -> d417d6d
2. [x] Push + PR #4 + `gh run` to prove it green -> d417d6d
3. [x] README + CLAUDE.md notes -> 5f31909

## Decisions
- 2026-07-14 — Single ubuntu runner, not an OS matrix — hooks are POSIX and the portability rule (not CI) owns Git Bash parity; a matrix is premature.

## Verification / evidence
- **CI ran green on GitHub** — PR #4 (`pull_request` trigger); run `29357682314` completed `success` in 7s; step log shows `hooks.test.sh: 41 passed, 0 failed`. Real end-to-end, not a local-only claim.
- Workflow is a single `ubuntu-latest` job: `actions/checkout@v4` + `bash hooks/hooks.test.sh`, no extra deps.

## Progress log
- 2026-07-14 `d417d6d` — CI workflow runs hooks.test.sh on push/PR to main.
- 2026-07-14 `5f31909` — CI documented in root/hooks CLAUDE.md + README.
