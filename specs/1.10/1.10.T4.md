---
id: 1.10.T4
title: Documentation — README, CLAUDE.md, migration guide, exit criteria
---

## Goal
Make the new distribution story the front-door story, and give existing users an explicit path off the old commands — the part the user flagged as essential.

- **README**: rewrite install to lead with `/plugin marketplace add liminalarc/flow-toolkit` + `/plugin install flow@…`; update every command reference to the new scheme (`/flow:run`, `/flow:hunt`, `/flow:init`, `/flow:lint`, `/flow:ship`, `/flow:review`, `/flow:pr`); keep the installer documented as the fallback; state the transition **exit criteria** (retire the installer only once the plugin registers hooks reliably across profiles AND all in-use projects migrated — no calendar date).
- **Migration guide** (README section or `MIGRATION.md`): the step sequence from T3 (add marketplace → install plugin → verify → prune old files → restart), plus the old→new command name mapping table.
- **CLAUDE.md**: update the distribution/architecture bullets (plugin primary; installer fallback), the Project Structure tree (`.claude-plugin/`, renamed skills/commands), and any command names referenced in the hierarchy.

## Done when
- [ ] README's install + command docs describe the plugin path and the new `/flow:*` names; installer shown as fallback; exit criteria stated with no fixed date.
- [ ] A migration guide gives the exact steps + an old→new name mapping table.
- [ ] Root CLAUDE.md (and any subdir CLAUDE.md) reflect the plugin distribution, `.claude-plugin/` layout, and renamed entry points; line caps still pass.

## Notes
- This is where 1.12's "how it works" docs will build from — leave the distribution story complete and current.
