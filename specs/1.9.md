---
id: 1.9
title: Migrate flow-pr to a skill with parallel-dimension sub-agents
links: [1.2, 1.8]
---

## Problem
`flow-pr` reviews a PR/branch diff across dimensions (spec/quality/tests) in one monolithic command. With the skill + parallel-reviewer pattern proven on `flow-review` (1.8), `flow-pr` is the second migration — validating the pattern on git-diff-bound, higher-frequency work.

## Value
As a flow user I want `/flow-pr` to load only the dimension I invoke and fan the review out to one agent per dimension against the diff, so that PR reviews are faster and the pattern is confirmed on diff-coupled work.

## Scope
**In:** convert `flow-pr` command → skill (`SKILL.md` + `reference/<dimension>.md`); one reviewer agent per dimension over the branch/PR diff; main-thread synthesis; entry point stays `/flow-pr`; reuse the reviewer agent from 1.8 where possible.
**Out:** `flow`/`flow-hunt` (1.11); plugin packaging (1.10).

## Acceptance criteria
- [ ] `flow-pr` is a skill; each dimension rubric is a `reference/*` file loaded per path.
- [ ] One reviewer agent per dimension runs against the diff in parallel; synthesized report.
- [ ] Spec-aware behavior (diff ↔ `specs/<id>.md`) preserved; `/flow-pr` entry point unchanged.
- [ ] README + docs updated.

## Plan (thin slices)
1. [ ] Extract dimension rubrics; author `SKILL.md`; reuse 1.8 reviewer agent.
2. [ ] Per-dimension dispatch over diff + synthesis.
3. [ ] Smoke-test on a real branch diff; update docs.

## Decisions
- YYYY-MM-DD — <decision + why>

## Verification / evidence
<how each AC is proven>

## Progress log
- YYYY-MM-DD `<sha>` — <what landed>
